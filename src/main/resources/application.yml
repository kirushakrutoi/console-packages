spring:
  sql:
    init:
      mode: never
  datasource:
    username: ${PACKAGES_DB_USERNAME}
    password: ${PACKAGES_DB_PASSWORD}
    url: ${PACKAGES_DB_URL}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: false

bot:
  api-url: https://api.telegram.org/
  path: https://95d5-89-31-38-68.ngrok-free.app
  name: packages
  token: 7640742907:AAF1sCUaPOXT-LPEkbgI3WDC1Q5U9ipei4o

dir:
  for:
    write: batches